[project]
name = "backend"
version = "0.1.0"
description = "TokeNitrate backend API service"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.4",
]

