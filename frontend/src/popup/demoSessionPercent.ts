/**
 * Placeholder session-usage percentage.
 *
 * TODO: replace once a real usage-budget model (session/context window
 * sizing) is defined. Input/output/total token values are already backed by
 * real estimates — only this figure remains a fixed demo value.
 */
export const DEMO_SESSION_PERCENT = 64
